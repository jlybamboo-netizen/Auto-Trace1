# AUTO TRACE · GitHub Pages 部署包

将本文件夹内的 `.github`、`site`、`README.md` 和 `UPDATE_POLICY.md` 放到 GitHub 仓库根目录。

进入仓库的 **Settings → Pages**，在 **Build and deployment → Source** 中选择 **GitHub Actions**。之后每次推送到 `main` 分支都会自动重新发布。

注意：不要把最外层的“GitHub部署包”文件夹整体上传成仓库中的二级目录。
